# 日志记录功能完整实现

## 概述
已为Neo4j知识图谱项目的节点查询、关系查询和通用查询页面添加了完整的日志记录功能。

## 数据库表结构

### 1. query_logs 表
- **主要用途**: 记录所有查询操作的详细信息
- **字段包括**:
  - 基础信息：用户ID、**用户角色**、查询类型、页面名称
  - 查询内容：查询内容、Cypher语句
  - 智能查询特有：自然语言查询、AI生成的Cypher、满意度评价
  - 执行结果：状态、结果数量、节点数、边数、执行时间、错误信息
  - 请求信息：**IP地址**、用户代理、会话ID

### 2. user_behavior_logs 表
- **主要用途**: 记录用户行为（如满意度评价）
- **字段包括**：用户ID、**用户角色**、行为类型、页面名称、行为详情（JSON格式）、**IP地址**、用户代理、会话ID

## 后端API接口

### 1. POST /api/logs/query
- **用途**: 创建查询日志记录
- **自动处理**: 用户ID、**用户角色**、**IP地址**、用户代理

### 2. POST /api/logs/behavior
- **用途**: 创建用户行为日志记录
- **自动处理**: 用户ID、**用户角色**、**IP地址**、用户代理

### 3. PATCH /api/logs/query/{log_id}/satisfaction
- **用途**: 更新查询日志的满意度评价

## 前端集成

### 1. 日志记录服务 (`logging.js`)
- **核心服务**: `loggingService`
- **主要方法**:
  - `logNodeQuery()` - 节点查询日志
  - `logRelationshipQuery()` - 关系查询日志
  - `logCypherQuery()` - Cypher查询日志
  - `logSmartQuery()` - 智能查询日志
  - `createBehaviorLog()` - 用户行为日志

### 2. 页面集成
#### 节点查询页面 (NodeQuery.vue)
- ✅ 导入日志服务
- ✅ 在 `performSearch` 方法中添加日志记录
- ✅ 记录查询参数、执行时间、结果数量
- ✅ 区分成功/失败状态

#### 关系查询页面 (RelationshipQuery.vue)
- ✅ 导入日志服务
- ✅ 在 `queryRelationship` 方法中添加日志记录
- ✅ 记录关系类型、过滤条件、执行时间、结果数量
- ✅ 区分成功/失败状态

#### 通用查询页面 (CypherQuery.vue)
- ✅ 导入日志服务
- ✅ 普通Cypher查询（`executeQuery`方法）日志记录
- ✅ 智能查询（`executeSmartQuery`方法）日志记录
- ✅ 满意度评价行为日志记录

## 记录的数据类型

### 1. 节点查询日志
```javascript
{
  query_type: 'node_query',
  page_name: 'NodeQuery',
  query_content: JSON.stringify({
    label: '选择的标签',
    searchQuery: '搜索关键词',
    limit: 50,
    mode: '查询模式'
  }),
  execution_status: 'success/error',
  result_count: 结果数量,
  execution_time_ms: 执行时间
}
```

### 2. 关系查询日志
```javascript
{
  query_type: 'relationship_query',
  page_name: 'RelationshipQuery',
  query_content: JSON.stringify({
    relationshipType: '关系类型',
    startNodeFilter: '起始节点过滤',
    endNodeFilter: '结束节点过滤',
    limit: 限制数量
  }),
  execution_status: 'success/error',
  result_count: 结果数量,
  execution_time_ms: 执行时间
}
```

### 3. Cypher查询日志
```javascript
{
  query_type: 'cypher_query',
  page_name: 'CypherQuery',
  query_content: 'Cypher查询语句',
  cypher_query: 'Cypher查询语句',
  execution_status: 'success/error',
  result_count: 记录数量,
  node_count: 节点数量,
  edge_count: 边数量,
  execution_time_ms: 执行时间
}
```

### 4. 智能查询日志
```javascript
{
  query_type: 'smart_query',
  page_name: 'CypherQuery',
  query_content: '自然语言查询',
  cypher_query: 'AI生成的Cypher',
  natural_language_query: '自然语言查询',
  generated_cypher: 'AI生成的Cypher',
  satisfaction_rating: null, // 初始为空，后续通过行为日志补充
  execution_status: 'success/error',
  result_count: 记录数量,
  node_count: 节点数量,
  edge_count: 边数量,
  execution_time_ms: 执行时间
}
```

### 5. 满意度评价行为日志
```javascript
{
  action_type: 'satisfaction_rating',
  page_name: 'CypherQuery',
  action_details: {
    rating: 'satisfied/unsatisfied',
    natural_language_query: '原始自然语言查询',
    generated_cypher: 'AI生成的Cypher',
    result_count: 结果数量,
    node_count: 节点数量,
    edge_count: 边数量
  }
}
```

## 特点和优势

1. **全面覆盖**: 涵盖所有查询页面的操作记录
2. **详细信息**: 记录查询参数、执行时间、结果统计等详细信息
3. **智能查询增强**: 特别针对智能查询记录AI生成的Cypher和用户满意度评价
4. **非侵入性**: 日志记录失败不影响主要功能运行
5. **自动化**: 自动记录用户信息、IP地址、时间戳等
6. **灵活扩展**: 支持行为日志记录用户的其他操作

## 使用说明

1. **启动前准备**:
   - 确保MySQL数据库已创建相关表（运行 `create_logs_table.sql`）
   - 后端服务已启动

2. **前端页面**:
   - 所有查询操作会自动记录日志
   - 智能查询的满意度评价会记录为行为日志

3. **数据分析**:
   - 可通过查询 `query_logs` 表分析用户查询行为
   - 可通过 `user_behavior_logs` 表分析用户满意度
   - 支持按时间、用户、查询类型等维度进行统计分析

## 后续可扩展功能

1. **日志查看界面**: 为管理员提供日志查看和统计界面
2. **性能分析**: 基于执行时间进行查询性能分析
3. **用户行为分析**: 基于日志数据分析用户使用模式
4. **智能查询优化**: 基于满意度评价改进AI查询生成算法