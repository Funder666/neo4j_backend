# 简化版日志记录功能说明

## 概述
简化后的日志记录系统使用单表设计，只保留核心必要的字段，去除了用户代理和会话ID等复杂字段。

## 数据库表结构

### query_logs 表（唯一的日志表）
- **主要用途**: 记录所有查询操作的详细信息
- **核心字段**:
  - **用户信息**: `user_id`、`user_role`、`ip_address`
  - **查询信息**: `query_type`、`page_name`、`query_content`、`cypher_query`
  - **智能查询**: `natural_language_query`、`generated_cypher`、`satisfaction_rating`
  - **执行结果**: `execution_status`、`result_count`、`node_count`、`edge_count`、`execution_time_ms`、`error_message`
  - **时间戳**: `created_at`、`updated_at`

## 后端实现

### API接口
- **POST /api/logs/query**: 创建查询日志记录
- **PATCH /api/logs/query/{log_id}/satisfaction**: 更新满意度评价

### 自动处理的字段
- `user_id`: 从认证信息中获取
- `user_role`: 从用户信息中获取 (admin/user)
- `ip_address`: 从请求信息中获取

## 前端集成

### 日志记录方法
- `logNodeQuery()` - 节点查询日志
- `logRelationshipQuery()` - 关系查询日志
- `logCypherQuery()` - Cypher查询日志
- `logSmartQuery()` - 智能查询日志

## 记录的查询类型

### 1. 节点查询 (node_query)
```json
{
  "query_type": "node_query",
  "page_name": "NodeQuery",
  "query_content": "{"label":"Word","searchQuery":"测试","limit":50,"mode":"general"}",
  "execution_status": "success",
  "result_count": 25,
  "execution_time_ms": 156
}
```

### 2. 关系查询 (relationship_query)
```json
{
  "query_type": "relationship_query",
  "page_name": "RelationshipQuery",
  "query_content": "{"relationshipType":"FROM_LEVEL","startNodeFilter":"","endNodeFilter":"","limit":100}",
  "execution_status": "success",
  "result_count": 15,
  "execution_time_ms": 234
}
```

### 3. Cypher查询 (cypher_query)
```json
{
  "query_type": "cypher_query",
  "page_name": "CypherQuery",
  "query_content": "MATCH (n:Word)-[r:FROM_LEVEL]->(l:InternationalLevel {value: '1'}) RETURN n, r, l limit 100",
  "cypher_query": "MATCH (n:Word)-[r:FROM_LEVEL]->(l:InternationalLevel {value: '1'}) RETURN n, r, l limit 100",
  "execution_status": "success",
  "result_count": 100,
  "node_count": 200,
  "edge_count": 100,
  "execution_time_ms": 342
}
```

### 4. 智能查询 (smart_query)

智能查询会产生**两条日志记录**：

#### 4.1 查询执行时的记录
```json
{
  "query_type": "smart_query",
  "page_name": "CypherQuery",
  "query_content": "国际中文教育中文水平1级的词语",
  "cypher_query": "MATCH (n:Word)-[:FROM_LEVEL]->(l:InternationalLevel {value: '1'}) RETURN n",
  "natural_language_query": "国际中文教育中文水平1级的词语",
  "generated_cypher": "MATCH (n:Word)-[:FROM_LEVEL]->(l:InternationalLevel {value: '1'}) RETURN n",
  "satisfaction_rating": null,  // 初始为空
  "execution_status": "success",
  "result_count": 504,
  "node_count": 504,
  "edge_count": 0,
  "execution_time_ms": 523  // 实际执行时间
}
```

#### 4.2 满意度评价时的记录
```json
{
  "query_type": "smart_query",
  "page_name": "CypherQuery",
  "query_content": "国际中文教育中文水平1级的词语",
  "cypher_query": "MATCH (n:Word)-[:FROM_LEVEL]->(l:InternationalLevel {value: '1'}) RETURN n",
  "natural_language_query": "国际中文教育中文水平1级的词语",
  "generated_cypher": "MATCH (n:Word)-[:FROM_LEVEL]->(l:InternationalLevel {value: '1'}) RETURN n",
  "satisfaction_rating": "satisfied",  // 用户评价
  "execution_status": "success",
  "result_count": 504,
  "node_count": 504,
  "edge_count": 0,
  "execution_time_ms": 0  // 标识为评价记录，非实际查询执行
}
```

## 设计优势

### 1. 简洁高效
- **单表设计**: 避免多表关联的复杂性
- **核心字段**: 只保留业务必需的关键信息
- **高性能**: 减少存储空间和查询复杂度

### 2. 实用导向
- **IP地址**: 便于安全审计和异常检测
- **用户角色**: 支持按权限级别统计分析
- **执行时间**: 便于性能监控和优化

### 3. 智能查询增强
- **完整记录**: 自然语言→Cypher转换的完整过程
- **双重记录**: 查询执行 + 满意度评价分别记录
- **满意度追踪**: 完整保留查询信息和用户评价的关联
- **结果统计**: 便于分析查询效果和用户满意度

## 部署步骤

1. **创建数据库表**:
   ```sql
   source create_simple_logs_table.sql
   ```

2. **启动后端服务**: 新代码会自动记录核心日志信息

3. **验证功能**: 执行查询操作，检查数据库中的日志记录

## 数据分析示例

### 满意度统计查询
```sql
-- 智能查询满意度分布
SELECT
    satisfaction_rating,
    COUNT(*) as count,
    ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER(), 2) as percentage
FROM query_logs
WHERE query_type = 'smart_query'
    AND satisfaction_rating IS NOT NULL
GROUP BY satisfaction_rating;
```

### 用户评价行为分析
```sql
-- 用户查询与评价的比率
SELECT
    user_role,
    COUNT(CASE WHEN satisfaction_rating IS NULL THEN 1 END) as queries_executed,
    COUNT(CASE WHEN satisfaction_rating IS NOT NULL THEN 1 END) as evaluations_submitted,
    ROUND(
        COUNT(CASE WHEN satisfaction_rating IS NOT NULL THEN 1 END) * 100.0 /
        COUNT(CASE WHEN satisfaction_rating IS NULL THEN 1 END), 2
    ) as evaluation_rate
FROM query_logs
WHERE query_type = 'smart_query'
GROUP BY user_role;
```

## 后续扩展建议

如果未来需要更详细的用户行为分析，可以考虑：
- 添加会话跟踪（如确有必要）
- 添加用户操作序列分析
- 基于满意度评价优化AI查询算法
- 实现满意度趋势分析和预警机制